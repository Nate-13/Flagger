// The visual theme lives as CSS custom properties in styles.js (so it can be
// swapped wholesale later). This module only carries the few values JS needs.

// Truthy sentinel passed as the second arg to flash() to render error styling.
export const DANGER = "error";
